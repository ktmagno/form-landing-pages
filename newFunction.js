$(document).ready(function () {
    //hover:bg-[#05A3EA]

$('#ipassword').on('keyup', () => {
    if $('#ipassword').val() == $('#cpassword').val()})
    $('#btn-signup').on('mouseenter', () => $('#btn-signup').css('background-color', 'red'));
});